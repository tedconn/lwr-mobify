import { LightningElement } from "lwc";

/**
 * This @slot comment is important, if it doesn't exist the slots will not be accessible
 *
 * @slot header This is the header slot
 * @slot footer This is the footer slot
 * @slot default This is the default slot
 */
export default class ScopedHeaderAndFooter extends LightningElement {

}
